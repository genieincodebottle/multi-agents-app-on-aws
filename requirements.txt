# Core (always needed)
python-dotenv>=1.0.0

# AWS Bedrock (default provider)
boto3>=1.42.0
botocore>=1.42.0

# Groq (free tier - uncomment if using LLM_PROVIDER=groq)
# groq>=0.12.0

# Gemini (free tier - uncomment if using LLM_PROVIDER=gemini)
# google-genai>=1.0.0
